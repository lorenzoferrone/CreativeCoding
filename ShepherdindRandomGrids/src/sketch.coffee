# {Grid} = require '../../Utils/Grid'
# {LocalModule} = require './localmodule'

setup =  () ->
    createCanvas windowWidth, windowHeight


draw = () ->









window.setup = setup
window.draw = draw
